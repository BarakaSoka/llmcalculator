version: 2
updates:
  # The package itself has no runtime dependencies, so the only supply-chain
  # surface worth watching automatically is the CI actions.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: monthly
    open-pull-requests-limit: 5
    commit-message:
      prefix: "ci"
    labels: ["dependencies", "ci"]
